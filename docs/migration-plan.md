# План миграции на канистры — исполнение архитектуры v3

> Детализация фаз M0–M5 из `canister-architecture.md` (ADR 0021) до уровня «можно начинать».
> Написан 2026-07-02. Правила наследуются: непрерывность для пользователей
> (архитектура §9), деньги трогаем последними, светофор владельца на каждом шаге.
>
> **Статус исполнения: M-1 ✅ выполнена 2026-07-04** (golden-векторы + `canister/` workspace +
> порты reputation/donation-парсера/tally с зелёным golden-паритетом + dfx). Подробности — §1
> (отметки ✓) и ROADMAP G3b. Следующая фаза — M0; от владельца к её мейннет-деплою нужен
> кошелёк циклов (~$25 в ICP).

---

## 0. Три правила чистоты (весь план стоит на них)

1. **Golden-паритет.** Канистра не «пишется заново» — она ПЕРЕПИСЫВАЕТ нашу уже оттестированную
   TS-логику (разбор доната, свёртка очков, машина споров). Из TS выгружаются тест-векторы
   (JSON: вход → ожидаемый выход), и Rust-код обязан проходить **те же самые векторы**
   байт-в-байт. Расхождение = стоп. Это превращает «переписали на другой язык» из риска в
   механическую проверяемую операцию.
2. **Переключение только по зелёной сверке.** Каждый кусок истины переезжает в три шага:
   канистра ДУБЛИРУЕТ (сервер — канон) → сверка горит зелёным N дней подряд → канистра
   СТАНОВИТСЯ каноном (сервер — дублёр/выключен). Ревизор `verify-export` сверяет три источника:
   Solana ↔ канистра ↔ сервер.
3. **Деньги — последними.** Редеплой эскроу (M2) начинается только после того, как M0–M1
   доказали чтение цепи, стабильность канистры и тресхолд-подпись на живом devnet.

---

## 1. M-1 «Подготовка» (до любого кода канистр)

**Репозиторий** — монорепо расширяется, ничего не ломая:

```
standing/
├── anchor/            Solana-программа (как есть)
├── canister/          НОВОЕ: Rust-workspace
│   ├── core/          core-канистра (индексер, журнал, споры)
│   ├── core.did       candid-интерфейс — публичный контракт канистры (аналог IDL)
│   └── dfx.json
├── testdata/golden/   НОВОЕ: тест-векторы из TS (см. ниже)
└── src/ …             как есть
```

**Golden-векторы** — новый скрипт `scripts/export-golden.ts` выгружает из существующих тестов
и живых devnet-данных:

- `donations.json` — реальные parsed-транзакции devnet → ожидаемый разбор (`extractDonation`);
- `reputation.json` — журналы → ожидаемые очки (`computePoints`/`computePointsAsOf`, включая
  дробные, клампы, asOf-границы);
- `disputes.json` — сценарии машины споров (все переходы, кворум, tally, репут-эффекты).

Rust-тесты канистры читают эти же файлы. `npm run golden && cargo test` — один светофор паритета.

**Тулчейн:** `dfx` (SDK ICP), Rust, **PocketIC** (локальный IC для интеграционных тестов —
стандарт DFINITY; есть тестовые тресхолд-ключи). Локальный дев-стенд: `dfx start` + канистра,
смотрящая на настоящий Solana devnet через outcalls.

**От владельца:** кошелёк циклов (~$20–30 в ICP токенах — хватит на месяцы M0–M2) + строка в
runbook про его пополнение.

**Выход из M-1:** пустая канистра деплоится локально и на mainnet ICP; golden-векторы
выгружены; `cargo test` зелёный на заглушках.

> **✓ Итог M-1 (2026-07-04).** Сделано сверх плана: не заглушки, а рабочие порты.
> - `scripts/export-golden.ts` (`npm run golden`) → `testdata/golden/{donations,reputation,disputes}.json`
>   (+README с нормативными соглашениями): 19+9 векторов разбора, 13+5 свёртки, 8 tally +
>   21 сценарий машины (все исходы, коды ошибок, граничные `>`/`<=`). Выход детерминирован
>   (повторный запуск — байт-в-байт).
> - `canister/` workspace: `dfx.json`, `core.did`, крейт `standing-core` (query `version`);
>   порты `computePoints*` (целые micro), `extractDonation`/`extractActivation`, `tally`
>   (целые micro — решение M2, yellow-paper §18.5-8). `cargo test`: 7 golden-тестов зелёные;
>   мутационная проверка (ломаем FEE_BPS/кламп → тесты падают) пройдена.
> - Тулчейн: dfx 0.32.0 (dfxvm), wasm32-таргет; грабля snap/XDG — в runbook.
> - Полный порт машины споров — M2 (сценарные векторы уже выгружены и ждут).
> - От владельца остаётся: кошелёк циклов (~$25 в ICP) — нужен к M0-деплою на mainnet ICP
>   (локальный стенд работает без него).

---

## 2. M0 «Канистра-наблюдатель» (ничего не решает, всё сверяется)

**Строим:** core-канистра v0:
- таймер → SOL RPC canister → подписи трежери-ATA → `getTransaction` (finalized) → разбор
  донатов/активаций (Rust-порт `extractDonation`, проверенный golden'ами) → журнал в stable
  memory; курсор там же;
- query-API: standing, лидерборд, журнал канала, экспорт (+ certified data);
- тресхолд-Ed25519: канистра получает свой Solana-адрес, подписывает ТЕСТОВУЮ memo-транзакцию
  на devnet через durable nonce — доказываем весь контур подписи ДО того, как он понадобится
  для денег.

**Сверяем:** `verify-export.ts --canister <id>`: журнал сервера == журнал канистры == цепочка.
Гоняется в CI/вручную; целевой критерий — **зелёный 7 дней подряд** при живом трафике devnet.

**Откат:** выключить канистру. От неё ничто не зависит.

**Светофоры владельца:** новая ✅-строка в ревизоре; отчёт с реальными цифрами (латентность
outcalls, цена циклов/день, подпись прошла).

> **✓ Прогресс M0 (2026-07-04): локальная половина работает.** Канистра-наблюдатель на локальной
> реплике (бесплатно): таймер 20 с → Solana devnet JSON-RPC через HTTPS-outcalls (прямой RPC;
> транспортный шов для SOL RPC canister заложен — `sol_rpc.rs`) → разбор донатов/активаций
> golden-портом → журнал+курсор в stable memory; query-API (`standing`/`leaderboard`/`journal`/
> `status`) + публичный JSON-экспорт `/export` через шлюз. Бэкфилл из первоисточника: 51 подпись
> трежери → 43 записи (8 чужих/эскроу-клеймов корректно отброшено), дыр retention нет.
> `verify-export --canister` сверяет три источника: **34/34 ончейн-доната канала совпали
> донат-в-донат** ✅. Найдено и классифицировано: 3 доната банкованы старой формулой
> (см. yellow-paper §18.5-9; решение о каноне — на M1).
> **✓ M0 технически завершена на локальном стенде (2026-07-04), включая тресхолд-подпись:**
> канистра получила собственный Solana-адрес `EekhckALUYi5r8ph1uuDQCaN4YHwnKSf7EqNyFgQi1SZ`
> (тресхолд-Ed25519, ключа целиком не существует; имя ключа — в init-конфиге: локально `key_1`)
> и ЖИВЬЁМ подписала и отправила devnet-транзакцию
> (`3G5RsrimaaUcyU8Jy5HFoLDnpejQtb7Z4fV1ArSD1ZrdKDM8QKDDNuUXmiRw9F25U9UcEP7zoFXKSzPoWHLJyexi`,
> memo от её адреса, err: none) — контур будущего резолвера M2 доказан ДО денег. Сборка
> Solana-tx — своя (`sol_tx.rs`, без solana-sdk), запинена эталонным тестом против web3.js.
> Ритуал наблюдения: гонять `verify-export --canister` при работе стенда (замена «7 дней
> зелёной сверки» до мейннета).
> **⏸ Мейннет отложен (решение владельца 2026-07-04, «доводить не скоро»)** — эти пункты
> уезжают в M5-гейты и НЕ блокируют M1/M2 на локальном стенде: mainnet-деплой + циклы,
> транспорт SOL RPC canister (нужен только консенсусу подсети из 13 узлов), durable nonce
> (обход консенсуса по свежему blockhash — тоже подсетная проблема), 7-дневная сверка на
> mainnet, certified data на экспорт, ключ `key_1` продовой подсети.

---

## 3. M1 «Канистра — канон чтения»

**Строим:**
- `IcpDataProvider` (пятая реализация DataProvider, гибрид как ChainDataProvider): репутация/
  лидерборды/журнал — из канистры (agent-js, query-вызовы бесплатны и быстры); всё остальное —
  как раньше. Включается env-флагом; экраны не меняются.
- Ончейн-конфиг канала в канистре: governance-параметры (пороги, кворум-коэффициент, окна,
  D_max) пишутся подписью владельца канала, изменения — с таймлоком. Студия получает секцию
  «Параметры споров» (подпись кошельком, как аттестация payout).
- Сервер продолжает вести свой журнал (дублёр) — сверка живёт.

**Откат:** флаг обратно — фронт читает сервер.

**Светофоры:** цифры в UI не изменились после переключения; сверка зелёная; параметры канала
меняются подписью и ждут таймлок.

> **✓ Прогресс M1 (2026-07-04): канон чтения переключён.** `IcpDataProvider`
> (`src/lib/data/icp-provider.ts`, yellow-paper §11.5): режим `NEXT_PUBLIC_DATA_SOURCE=icp` —
> стендинг и лидерборд браузер читает НАПРЯМУЮ из канистры (HTTP-экспорт `/standing`,
> `/leaderboard` с CORS), имена/косметика — join с кожей, тиры — по конфигу канала; экраны не
> тронуты (DataProvider-замок). Серверный индексер продолжает работать (дублёр, сверка живёт).
> Откат проверен конструктивно: `=chain`. Светофор «цифры не изменились» на dev-стенде
> НЕ выполняется намеренно — состав разрыва померен и разобран в yellow-paper §18.5-8a
> (изъятый CR-1 `ADMIN_VOID` −65, mock-эпоха +2, легаси-округления +0.4; на mainnet классов нет).
> **✓ Governance-параметры в канистре (2026-07-04, вторая половина M1, сторона канистры).**
> `canister/core/src/governance.rs`: параметры споров канала (`minReputationToDispute`,
> `minWeightToVote`, кворум-K, окна, D_max — спека голосования §7) хранятся в stable memory;
> запись — ТОЛЬКО ed25519-подписью владельца канала (канонное сообщение в стиле payout-аттестации,
> пин байт-в-байт в unit-тесте), **владелец выводится ИЗ ЦЕПОЧКИ** (плательщик активации в
> журнале — сервер в праве на запись не участвует); версия строго растёт (анти-replay); вступление
> с таймлоком ≥ полного цикла спора действующих правил (§8.9, пол 300 с). Транспорт — HTTP-шлюз
> канистры: GET/POST `/dispute-params` (POST апгрейдится в `http_request_update`), плюс candid
> `dispute_params`. Проверено ЖИВЬЁМ на devnet (тест-канал `gov-smoke-1`): активация → владелец
> из цепочки → подписанная запись принята → replay/подделка отвергнуты → таймлок.
> **✓ M1 ЗАКРЫТА (2026-07-04): студия получила секцию «Параметры споров».**
> `DisputeParamsSection` в настройках студии (виден в icp-режиме): форма в человеческих единицах
> (очки/минуты/USDC), кнопка «Подписать и отправить в канистру» — подпись кошельком канонического
> сообщения (`src/lib/chain/dispute-params.ts`; байт-в-байт паритет с Rust запинен ПАРНЫМИ
> тестами: dispute-params.test.ts ↔ governance.rs). Методы `getDisputeParams`/`setDisputeParams` —
> опциональные в DataProvider-замке (есть только у IcpDataProvider; UI проверяет наличие).
> Показ pending-изменения с датой вступления и версией. Параметры начнёт ПОТРЕБЛЯТЬ машина
> споров при M2-порте.
> **Дополнение 2026-07-05:** профиль донора (/me, /u) тоже переведён на канон — `/donor?address=`
> в канистре + слияние в `getDonorOverview` (кожа с сервера, цифры из канистры; замечено
> владельцем на живом стенде: строки каналов показывали серверные очки).

---

## 4. M2 «Споры» — первое и единственное касание денег

**Строим:**
- Модуль споров в канистре: `open_dispute` (депозит: пошлина+залог), приём голосов-подписей
  (формат из спеки, ed25519-проверка), конверты для ≥$50, табло, финализация по таймеру,
  репут-эффекты (+10/−50, только активный проигрыш), D_max, √-кворум. Всё по
  `games/onchain-voting-spec.md` v1.1 — экономика уже спроектирована и отревьюена.
- **Редеплой эскроу-программы**: `RESOLVER` = тресхолд-адрес канистры. Одним релизом с
  mainnet-пинами ESC-7 (mint) и ESC-9 (прод-окна) — три давно висящих пункта закрываются разом.
- Красивая деталь контракта, которая делает переход бесшовным: `resolver` записывается в каждый
  эскроу **при fund** — старые лётные эскроу доживают со старым резолвером, новые рождаются с
  канистрой. Миграции живых денег НЕ существует как задачи. Откат симметричен: редеплой с
  прежней константой — новые эскроу вернутся к старой схеме, канистровые доживут у канистры
  (плюс вечный страховочный `resolve_timeout`).
- Кнопки резолвера и `ESCROW_RESOLVER` удаляются из UI/env.

**Депозит спора:** на devnet — devnet-USDC в эскроу-подобный PDA ИЛИ упрощённо учётом в
канистре; решение на входе в M2 (открытый вопрос спеки №1).

**Светофоры:** сценарий спора из manual-testing тремя кошельками — исход приходит из канистры
без нашего участия; смоук-скрипт `dispute-smoke` (аналог escrow-smoke) против живой связки;
нагрузочная мини-репетиция (10k синтетических голосов).

> **✓ Прогресс M2 (2026-07-05): машина споров портирована в канистру и golden-доказана.**
> `canister/core/src/disputes.rs` — полный порт machine.ts (create/accept/reject/cancel/
> markDone/raiseDispute/castVote/tally/dueResolution/applyResolution/repEffects/claim) в
> нативных единицах канистры: epoch-мс + ЦЕЛЫЕ micro-очки (float исключён — §18.5-9);
> окна — параметром (их даст governance-конфиг канала). Golden-раннер прогоняет все
> 21 сценарий эталона шаг-в-шаг: снапшоты состояний, коды ошибок GameBusError, границы
> `>`/`<=`, репут-эффекты; текст-поля (кожа) — вне порта намеренно. Мутационная проверка
> (граница грейса ±1 мс, штраф 49≠50) — раннер ловит.
> **✓ Решения владельца по экономике споров (2026-07-05, вторая ревизия):**
> (1) **депозит/пошлина/залог УДАЛЕНЫ ЦЕЛИКОМ** — денежных наказаний нет, «сжигаем только
> репутацию» (первоначальное «депозит учётом» отменено тем же днём); (2) **кворум — фикс от
> стримера** (governance `quorum_micro`, дефолт 1 очко), √-формула убрана; (3) **табло открыто**.
> Расхождения со спекой v1.1 записаны: баннер в самой спеке + yellow-paper §18.5-8b/8c.
>
> **✓ Арбитр-модуль построен (2026-07-05, `canister/core/src/arbiter.rs`):** канистра принимает
> споры и голоса и выносит вердикты БЕЗ площадки. Открытие — подписью инициатора, эскроу
> читается ИЗ ЦЕПОЧКИ (getAccountInfo + проверка владельца-программы; раскладка аккаунта
> запинена живой devnet-фикстурой); вес/право голоса — снимок журнала канистры на момент
> открытия; пороги/окно — governance-параметры канала; √-кворум `max(1, ⌈K·√USDC⌉)` (пины
> $1→2, $100→20, $10k→200); голос — подпись канон-сообщения (выбор ВНУТРИ подписанного текста;
> парные пины TS↔Rust — dispute-vote.test.ts ↔ arbiter.rs); **табло и голоса открыты живьём —
> решение владельца 2026-07-05** (спека прятала до вердикта против стадности/прицельного
> подкупа, атака §8.5; риск принят; конверты для крупных споров — мейннет-план);
> финализация таймером → вердикт + учёт депозита (залог сгорает только при активном проигрыше)
> + записи журнала GAME_DONATION/DISPUTE_WON/DISPUTE_LOST (свёртки стали знаковыми с клампом).
> HTTP: POST /dispute/open, /dispute/vote; GET /dispute, /disputes. Полный цикл покрыт
> юнит-тестом (открытие→голоса→финализация→эффекты) + живые негативные смоуки.
> Известное допущение v1: привязка эскроу→канал через `streamer == владелец канала`.
> **✓ ТРАСТЛЕСС-КОНТУР ЗАМКНУТ (2026-07-05): спор решается канистрой без площадки — доказано
> живым смоуком на devnet.** Сделано:
> - **Эскроу-программа передеплоена**: `RESOLVER` = тресхолд-адрес канистры
>   (`EekhckAL…i1SZ`; upgrade-подпись `3iuD1Rpx…`). Поле per-escrow: лётные эскроу доживают
>   со старым операторским резолвером, новые рождаются с канистрой — миграции денег нет.
> - **Канистра шлёт ончейн сама** (общий сборщик Solana-tx `sol_tx::build_message`, эталон
>   web3.js): `mark_disputed` при открытии спора (блокирует resolve_timeout на голосование),
>   `resolve_dispute` по вердикту; недоставленное ретраится каждым тиком таймера. Плюс
>   операционный рычаг `withdraw_sol` (газ резолвера, только контроллер).
> - **`scripts/dispute-smoke.ts`** — штатный светофор: канал→вес→fund→accept→markDone→
>   финализация→спор (подпись)→mark_disputed тресхолдом→голос→вердикт→resolve_dispute
>   тресхолдом→ончейн Resolved(ToDonor)→claim вернул $1→DISPUTE_WON +10 в журнале. ПРОЙДЕН.
> - **Кросс-языковые пины канон-сообщений переведены на общую фикстуру**
>   `testdata/golden/messages.json` (порождает `npm run golden`, читают Rust-тесты) — после
>   пойманного на смоуке дрейфа текста TS↔Rust (тексты теперь не могут разойтись молча).
> **✓ M2 ЗАКРЫТА (2026-07-05).** UI: те же кнопки «Оспорить»/«Голос» панели задания в icp-режиме
> идут В КАНИСТРУ (IcpDataProvider перехватывает `raiseDispute`/`vote` для chain-задач: подпись
> кошельком канон-сообщения → арбитр); карточка получает оверлей `CanisterDisputeBlock` —
> открытое табло, отсчёт, вердикт со ссылкой на подпись резолвера. ПЕРВОЕ УДАЛЕНИЕ старого кода:
> ручные кнопки резолвера, ветки `markDisputed`/`resolveDispute` chain-провайдера и
> `ESCROW_RESOLVER`/`NEXT_PUBLIC_ESCROW_RESOLVER` — удалены (yellow-paper §16/§18.2 обновлены).
> Задачи без эскроу (mock/api-эпоха) спорятся по-старому оффчейн — ветка сохранена.

---

## 5. M3 «Фронт на asset-канистре»

- Конверсия в SPA: решение Vite+React Router vs `next` static export — по фактическому объёму
  боли (экраны уже клиентские, DataProvider абстрагирует данные; переезжают только серверные
  route-handlers: трастлесс → канистра, кожа → наш API).
- Asset-канистра + наш домен (регистрация в HTTP-gateway, TLS автоматом), сертифицированная
  раздача.
- Кожа-API остаётся на сервере (модерация, тексты, /ops) — фронт ходит в оба.

**Откат:** DNS обратно на старый хостинг.

---

## 6. M4 «Governance» и M5 «Мейннет»

- M4: контроллеры core-канистры → мультисиг; политика апгрейдов с таймлоком; процедура
  публична. (Blackhole/SNS — по зрелости, отдельное решение.)
- M5: полный аудит (эскроу-редеплой + core-канистра — бюджетируется $10–50k), нагрузочная
  репетиция 100k голосов, юр-мастер-переменная, mainnet-деплой. Kожа-канистра (M6, §4.1
  архитектуры) — после всего, опционально.

---

## 7. Операционка (дельта в runbook по ходу фаз)

- Циклы: мониторинг балансов канистр + автопополнение; freezing threshold = сайт-риск №1 новой
  эпохи (деньги пользователей НЕ страдают — `resolve_timeout`, но индексация встанет).
- Деплой канистры: `dfx deploy` + запись версии/hash в ADR-лог апгрейдов.
- Ключи: cycles-кошелёк владельца — в тот же ряд, что treasury/anchor (runbook «Ключи»).
- Сверка: `verify-export --canister` в ежедневный ритуал (потом CI).

---

## 8. Что нужно от владельца по фазам

| Фаза | Решения | Деньги | Твоя проверка |
|---|---|---|---|
| M-1 | — | ~$25 в ICP (циклы) | `cargo test` зелёный на golden |
| M0 | — | — | ✅-строка ревизора 7 дней; отчёт цифр |
| M1 | дефолты параметров споров по каналам | — | UI не изменился; таймлок работает |
| M2 | форма депозита на devnet; ревью спеки-дельт | — | спор без нас; smoke; мини-нагрузка |
| M3 | Vite vs next-export (покажу сравнение) | — | сайт с канистры, тот же домен |
| M4 | состав мультисига | — | публичная процедура апгрейда |
| M5 | бюджет аудита, юрисдикция | $10–50k+ аудит, юрист | отчёты внешних |

Порядок работ измеряется сессиями, не месяцами: M-1 ≈ 1 сессия, M0 ≈ 2–4, M1 ≈ 1–2, M2 ≈ 3–5,
M3 ≈ 2–4; M4–M5 — процедуры и внешние подрядчики.

---

## 9. Чего в плане НЕТ намеренно

- Переписывания экранов (DataProvider-замок), миграции пользователей (непрерывность §9),
  миграции лётных эскроу (resolver per-escrow), изменения донат-пути (священен), переноса
  модерации/текстов (кожа остаётся, §4.1), больших взрывных релизов (каждая фаза — маленькая,
  со своим откатом).
