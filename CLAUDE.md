# CLAUDE.md — точка входа

> Это рабочий контекст для Claude Code. Прочитай этот файл целиком перед началом любой задачи.
> Кодовое имя проекта — **Standing** (заглушка, см. `README.md`).

---

## 1. Что мы строим (в одном абзаце)

Репутационная платформа для стримеров. Зритель донатит стримеру в USDC на Solana и за это
накапливает **локальную репутацию** внутри комьюнити именно этого стримера (у каждого стримера —
своя, глобального рейтинга нет). Эта репутация — непродаваемое, непередаваемое вычисляемое
число — и есть продукт. Поверх неё позже сядут мини-игры (отдельные документы, в этой версии их нет).
Текущий объём — **ядро без игр**: идентичность, донаты, локальная репутация, статус (тиры/бейджи/
лидерборды), модерация текста, баны. Полная спека ядра — `docs/core-spec.md`.

---

## 2. Порядок строительства (это главное)

Строим **строго по фазам**, и **сначала весь фронтенд на мок-данных**, потом бэкенд, потом крипта.
Полная дорожная карта с чек-листами — `ROADMAP.md`. Кратко:

| Фаза | Что | Где спека |
|------|-----|-----------|
| **0** | Скелет репо + дизайн-система + токены | `frontend/design-system.md` |
| **1** | Весь UI/UX на мок-данных (zero backend) | `frontend/*` |
| **2** | Бэкенд: репутационный движок, API, модерация | `backend/spec.md` |
| **3** | Крипта: кошелёк, донат-транзакция, индексер | `crypto/spec.md` |

**Почему фронт первый:** цель — вылизать UI/UX до того, как платить сложностью бэкенда и ончейна.
Чтобы фронт не пришлось переписывать, см. §3 — он пишется против абстракции, а не против сети.

---

## 3. Архитектурный замок: `DataProvider` (не нарушай)

Фронтенд **никогда** не вызывает `fetch`, RPC или Solana напрямую. Весь доступ к данным идёт через
один типизированный интерфейс `DataProvider` (контракт — `frontend/mock-data.md`).

```
UI компоненты  ──►  useData() хук  ──►  DataProvider (интерфейс)
                                          ├─ MockDataProvider   (Фаза 1)  ← пишем сейчас
                                          ├─ ApiDataProvider    (Фаза 2)  ← бэкенд
                                          └─ ChainDataProvider  (Фаза 3)  ← кошелёк + индексер
```

Реализация выбирается через ENV-флаг (`NEXT_PUBLIC_DATA_SOURCE=mock|api|chain`). Переход между
фазами = добавить реализацию интерфейса, **не трогая ни один экран**. Если ты ловишь себя на том,
что зовёшь сеть из компонента — стоп, это нарушение контракта.

---

## 4. Золотые инварианты (нарушение = баг, не фича)

Эти свойства защищают проект юридически и экономически (`docs/legal-and-risk.md`). Любой код,
который их ломает, неверен, даже если «так проще».

1. **Некастодиальность.** Оператор никогда не держит деньги доната и не имеет ключа, способного
   двигать чужие средства. Донат идёт донор → стример напрямую (минус комиссия). Эскроу в ядре нет.
2. **Деньги финальны.** Нет чарджбэков, нет возвратов в ядре. Донат подтверждён → он необратим.
   Скрытие текста — это решение о публикации, а не о деньгах.
3. **Репутация непередаваема и непродаваема.** Это вычисляемое число, привязанное к адресу, а не
   токен. Никакого перевода, обмена, рынка. Это юридический замок (нет «ценной бумаги»).
4. **Репутация детерминирована и перевычислима.** Любой может скачать историю донатов + конфиг
   стримера и независимо получить ту же цифру. Сервер не «решает», он считает по открытой формуле.
5. **Репутация в ядре только растёт, и оператор её НЕ редактирует.** Ручного списания нет — число
   всегда честная свёртка журнала (§4.4). Единственная отрицательная дельта — протокольный `DISPUTE_LOST`
   (проигранный ложный спор, слой игр). Наказание нарушителя — БЛОК кошелька/канала, который обесценивает
   репутацию, не трогая само число (аудит CR-1; раньше здесь был операторский `ADMIN_VOID` — убран).
6. **Текст приватен до показа.** Текст доната по умолчанию виден только стримеру и его модераторам.
   В публичный фид / на оверлей он попадает только после явного «Показать».
7. **Деньги ≠ показ.** Репутация и деньги начисляются сразу и не зависят от судьбы текста.

---

## 5. Стек (рекомендация, см. ADR `decisions/`)

- **Frontend:** Next.js (App Router) + TypeScript + Tailwind. Компоненты — собственная мини-библиотека
  поверх Radix primitives (см. `frontend/components.md`). Состояние — TanStack Query поверх `DataProvider`.
- **Backend (Фаза 2):** TypeScript-сервис (Next route handlers / отдельный Node-сервис), Postgres,
  детерминированный репутационный движок как чистая функция (`backend/spec.md`).
- **Crypto (Фаза 3):** `@solana/wallet-adapter-react` (+ `-react-ui`) для подключения кошелька и
  Sign-In-With-Solana; `@solana/kit` / `gill` для построения транзакций (SPL USDC transfer + memo + ATA).
  Детали и причины выбора — `crypto/spec.md`.

Версии пакетов и цены внешних API меняются — **сверяй перед установкой** (`product-self-knowledge`
skill для всего, что касается Anthropic; web-поиск для Solana/модерации).

---

## 6. Карта репозитория

```
standing/
├── CLAUDE.md                 ← ты здесь
├── README.md                 человекочитаемый обзор + кодовое имя
├── ROADMAP.md                фазы, чек-листы, критерии выхода
├── docs/                     ЧТО строим (механика, неизменная)
│   ├── core-spec.md          чистая спека ядра v0.1 (источник истины по механике)
│   ├── architecture.md       ончейн/оффчейн, потоки данных
│   ├── data-model.md         сущности + журнал репутации + версионирование конфига
│   ├── legal-and-risk.md     карта рисков + инварианты §4
│   ├── audit-map.md          инвариант §4 → код, границы доверия, находки → ADR (точка входа аудита)
│   ├── glossary.md           термины
│   └── games/escrow-task-spec.md  спека первой мини-игры (v1.1; шов с ядром — ADR 0015)
├── frontend/                 КАК выглядит и работает UI (Фаза 1)
│   ├── spec.md               принципы, IA, контракт с данными
│   ├── design-system.md      токены: цвет, типографика, ритм, движение
│   ├── screens.md            экран за экраном + состояния
│   ├── components.md         библиотека компонентов
│   ├── user-flows.md         потоки: донатер, стример, модератор, оператор
│   └── mock-data.md          интерфейс DataProvider + фикстуры (TS)
├── backend/spec.md           Фаза 2: движок, API, модерация, конфиг
├── crypto/spec.md            Фаза 3: кошелёк, транзакция, индексер
└── decisions/                ADR — записи архитектурных решений
    ├── 0001-frontend-first-with-mock-data.md
    └── 0002-core-v0.1-clarifications.md   создание канала, единицы денег, standing, 1 канал/кошелёк
```

---

## 7. Конвенции

- **Язык:** документы и UI-копирайт — русский; код, идентификаторы, имена файлов/папок, схемы, типы,
  коммиты — английский. Не смешивай.
- **Деньги и адреса — всегда моноширинным шрифтом** в UI. Это структурное правило (числа = данные).
- **Имена в UI — от лица пользователя**, а не системы. Кнопка говорит, что произойдёт: «Показать»,
  «Задонатить», «Забанить канал», а не «Submit» / «Execute».
- **Никаких заглушек-обманок.** Если фича не готова — явный empty/disabled state с объяснением, а не
  фейковая кнопка.
- **Деньги и репутация — в минимальных целочисленных единицах** в коде (USDC = 6 знаков → храним
  micro-USDC `bigint`; очки репутации — целые). Форматирование только на границе UI.
- **Каждая фаза заканчивается обновлением `ROADMAP.md`** (отметить выполненные пункты) и, если было
  архитектурное решение, новым файлом в `decisions/`.

---

## 8. С чего начать прямо сейчас

1. Прочитай `ROADMAP.md` → Фаза 0 и Фаза 1.
2. Прочитай `frontend/spec.md`, `frontend/design-system.md`, `frontend/mock-data.md`.
3. Подними скелет (Фаза 0), затем строй экраны из `frontend/screens.md` против `MockDataProvider`.
4. Не переходи к Фазе 2, пока не выполнены критерии выхода Фазы 1 в `ROADMAP.md`.
